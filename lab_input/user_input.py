#!/usr/bin/env python3

first_name = input('Enter your first name: ')

last_name = input('Enter your last name: ')

day_of_week = input('Enter the day of the week: ')

print('Hello,', first_name, last_name + '!', 'Happy', day_of_week + '!')
